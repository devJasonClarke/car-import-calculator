import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Jamaica Car Import Tax Calculator | Dev Jason Clarke",
  description: "Free Jamaica vehicle import cost calculator for cars, SUVs, pickups, and hybrids from Japan. Calculate CIF value, import duties, GCT, SCTA, and all fees with real-time USD to JMD conversion. Accurate tax rates and comprehensive cost breakdown. Built by Dev Jason Clarke.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
